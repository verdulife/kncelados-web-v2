---
import { getShorts } from "@/lib/getShorts";

const video_ids = await getShorts();
---

<section
  id="last-videos"
  class="mt-24 flex w-full flex-col items-center justify-center gap-6 text-center"
>
  <h2 class="text-center text-5xl lg:text-7xl">Últimos shorts</h2>
  <p class="mt-4 max-w-4xl self-center px-4 text-lg lg:text-2xl">
    Si quieres estar al día con los últimos shorts, aquí tienes unos cuantos. Si
    quieres más videos cortos, visita nuestras redes.
  </p>

  <ul
    class="relative mt-8 grid w-full max-w-7xl grid-cols-2 gap-6 px-6 md:grid-cols-4"
  >
    {
      video_ids.map((id) => (
        <li class="relative w-full snap-start">
          <lite-youtube
            class="aspect-9/16 w-full shrink-0 rounded-xl"
            videoid={id}
            videoplay="Reproducir"
            short
          />
        </li>
      ))
    }
  </ul>
</section>

<style>
  ul {
    scrollbar-width: none;
  }
</style>